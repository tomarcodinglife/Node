
let a = 5;
let b = 10;

// Synchronous function to add two numbers
function addSync(x, y) {
    return x + y;
}   

function main() {
    console.log("Synchronous Addition Result:", addSync(a, b));
}

main(); 
// Output will be logged immediately
// Synchronous Addition Result: 15